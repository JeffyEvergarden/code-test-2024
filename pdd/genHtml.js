String.prototype.genHtml = function () {
    const str = this


    const modules = str.split('>');
    // ['div.hello', 'p#fake', 'div.hello']

    function genModule(i) {

        if (i > modules.length) {
            return modules.
        }

        const curNode = ''


        const child = genModule(i + 1)

    }
}